{
  "name": "matic",
  "version": "1.0.0",
  "description": "matic home automation system",
  "main": "server.js",
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"nodemon server\" \"npm run client\"",
    "start": "node server.js"
  },
  "author": "jhun wulf sabala",
  "license": "ISC",
  "dependencies": {
    "body-parse": "^0.1.0",
    "concurrently": "^6.3.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "helmet": "^4.6.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.14"
  }
}
